defaults:
  - base_dataset
  - view_sampler: bounded

name: argoverse
roots: [/mnt/nfs/SpatialAI/Datasets/av2_temp/]

input_image_shape: [256, 256]
original_image_shape: [2048, 1550]
cameras_are_circular: false

baseline_min: 1e-3
baseline_max: 1e2
max_fov: 100.0
avg_pose: false

rescale_to_1cube: true
make_baseline_1: false
intr_augment: true